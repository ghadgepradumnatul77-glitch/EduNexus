# Frontend environment for Docker
# VITE_ prefix makes vars available in the browser bundle

# Vite config reads this to set the /api proxy target inside Docker
VITE_BACKEND_URL=http://backend:5000
VITE_APP_NAME=EduNexus

